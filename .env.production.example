# --- PRODUCTION ENVIRONMENT TEMPLATE ---
# Use these values in your CI/CD provider or Production Server

# Core
NODE_ENV=production
PORT=5001

# Database
# Use a production-grade MongoDB (e.g. Atlas with IP whitelisting)
MONGODB_URI=mongodb+srv://<user>:<password>@prod-cluster.mongodb.net/chat_db

# Security
# Use a long, random string (e.g. `openssl rand -base64 32`)
JWT_SECRET=your_super_stable_production_secret

# AI
OPEN_ROUTER=your_production_open_router_key

# Frontend Connectivity
# If serving from the same domain/port:
FRONTEND_URL=https://your-domain.com
VITE_API_URL=https://your-domain.com

# If using a separate frontend domain (e.g. Vercel):
# FRONTEND_URL=https://chat.yourdomain.com
# VITE_API_URL=https://api.yourdomain.com
